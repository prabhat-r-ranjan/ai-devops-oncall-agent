apiVersion: v1
kind: Service
metadata:
  name: ai-devops-oncall-agent
  namespace: ai-devops
  labels:
    app: ai-devops-oncall-agent
spec:
  type: ClusterIP
  selector:
    app: ai-devops-oncall-agent
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP